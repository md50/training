name: First workflow 
on: [push]

jobs:
    run-shell-commands:
       run-on: ubuntu-latest 
       steps: 
        - name: echo string 
          run: echo "Hello World"
        - name: Multiline
          run: |
            node -v 
            npm -v