name: First workflow 
on: [workflow_dispatch, push]
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "Hello World"
      - name: checkout 
        uses: actions/checkout@v6
      - name: Multiline
        run: |
          node -v

          npm -v
          
          ls 
